import React, { Component } from 'react';
import './loading.css';

class Loading extends Component {
    render() {
        return (
            <h2>Loading</h2>
        );
    }
}

export default Loading;